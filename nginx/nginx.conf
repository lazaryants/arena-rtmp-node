user www-data;
worker_processes auto;
pid /run/nginx.pid;
include /etc/nginx/modules-enabled/*.conf;

events {
    worker_connections 768;
}

http {
    sendfile on;
    tcp_nopush on;
    types_hash_max_size 2048;
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
    gzip on;
    include /etc/nginx/conf.d/*.conf;
    include /etc/nginx/sites-enabled/*;
}

rtmp {
    server {
        listen 1935;
        chunk_size 4096;

        # Площадка 1
        application place1 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place1;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 2
        application place2 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place2;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 3
        application place3 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place3;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 4
        application place4 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place4;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 5
        application place5 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place5;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 6
        application place6 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place6;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 7
        application place7 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place7;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 8
        application place8 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place8;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 9
        application place9 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place9;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 10
        application place10 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place10;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 11
        application place11 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place11;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 12
        application place12 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place12;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 13
        application place13 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place13;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 14
        application place14 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place14;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 15
        application place15 {
            live on;
            record off;

            notify_method post;
            on_publish http://127.0.0.1:8080/auth;

            hls on;
            hls_path /var/www/hls/place15;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }

        # Площадка 16
        application place16 {
            live on;
            record off;
            hls on;
            hls_path /var/www/hls/place16;
            hls_fragment 4;
            hls_playlist_length 16;
            hls_cleanup on;
            hls_type live;
        }
    }
}
